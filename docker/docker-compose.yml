version: "3.9"

services:

  api-gateway:
    build:
      context: ../backend/api-gateway
      dockerfile: ../../docker/gateway.Dockerfile
    ports:
      - "4000:4000"

  clientes-service:
    build:
      context: ../backend/services/clientes-service
      dockerfile: ../../../docker/service.Dockerfile
    ports:
      - "3001:3001"